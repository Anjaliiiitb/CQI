/**
 * Controller for showing the pin hierarchy
 */
define(['app'], function(app){
		app.controller("pinHierarchyCtrl", ['$scope', '$rootScope', '$http','pinHierarchyService', 'globalService', 
		                               function($scope, $rootScope, $http, pinHierarchyService, globalService) {
			
			$scope.testVal = "Pin Hierarchy"
           
		
		}]);
		
});

